# Task
Migrate this local automation to run on the cloud with daily scheduling. No computer needed.

## Do in order:
1. Read all project files. Summarize: what it does, deps, how it's triggered, credentials needed.

2. Adapt code:
   - Remove hardcoded paths/credentials
   - Use env vars for all secrets
   - Clear entry point (main.py / index.js)
   - Add start/end/error logs

3. Choose best free platform for this project (GitHub Actions preferred if simple script).
   Create required config files:
   - Actions → .github/workflows/automation.yml
   - Railway/Render → Dockerfile + config
   - Cloud Run → Dockerfile + gcloud commands

4. Create .env.example (no real values). Confirm .env is in .gitignore.

5. Update README: deploy steps, set secrets, verify run, change schedule.

## Rules:
- Keep business logic identical
- Free solutions only
- Simplest solution wins
- Comments in Portuguese

## Deliver:
adapted code · scheduler config · .env.example · README · list of manual steps for me to do